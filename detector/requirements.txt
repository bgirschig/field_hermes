# This requirements file is edited manually to reflect the top-level modules
# imported by the scripts.
# It allows to do a 'clean install' (without installing since-removed dependencies)

opencv-python==4.1.1.26
numpy==1.17.2
scipy==1.3.1
websockets==9.1